// GENERATED by scripts/build.mjs from data/ — do not edit by hand.
// Average of the ECB daily euro reference rates over the window in data/ecb-rates.csv, as units of each currency per 1 EUR.
// Data as of 2026-09-25.

export const FX = {
  "base": "EUR",
  "method": "Arithmetic mean of ECB daily reference rates",
  "from": "2025-09-25",
  "to": "2026-09-24",
  "observations": 255,
  "rates": {
    "CZK": 24.2793,
    "DKK": 7.4718,
    "HUF": 373.7045,
    "PLN": 4.2606,
    "SEK": 10.9067,
    "USD": 1.1622,
    "EUR": 1
  }
};

/** Convert an amount in `currency` to euros at the average rate (or a custom USD rate). */
export function toEUR(amount, currency, usdPerEur = FX.rates.USD) {
  if (amount == null) return null;
  if (currency === 'EUR') return amount;
  const rate = currency === 'USD' ? usdPerEur : FX.rates[currency];
  return rate ? amount / rate : null;
}

export function fmtRate(currency) {
  const r = FX.rates[currency];
  return r >= 100 ? r.toFixed(0) : r.toFixed(2);
}
